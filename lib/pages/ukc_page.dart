import 'package:flutter/material.dart';

class UKCPage extends StatelessWidget {
  const UKCPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Halaman UKC Calculation"),
      ),
    );
  }
}
